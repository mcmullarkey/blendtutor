% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blendtutor.R
\name{start_lesson}
\alias{start_lesson}
\title{Start an interactive coding lesson}
\usage{
start_lesson(lesson_name = "add_two_numbers", model = NULL)
}
\arguments{
\item{lesson_name}{Lesson reference in one of three forms:
\describe{
\item{Bare name}{\code{"add_two_numbers"} — searches all packages, errors
if the name is found in multiple packages}
\item{Qualified name}{\code{"mypkg::add_two_numbers"} — searches a specific
package only}
\item{File path}{\code{"path/to/lesson.yaml"} — loads a local YAML file
directly (useful during lesson development)}
}}

\item{model}{Model to use (optional; defaults to Fireworks default if not specified)}
}
\value{
Invisible NULL. Displays lesson information and waits for code submission.
}
\description{
Loads a lesson and begins an interactive learning session with AI-powered
feedback via the Fireworks API.
}
\details{
Lessons are discovered automatically from blendtutor and any installed package
that depends on blendtutor and contains YAML files in \verb{inst/lessons/}.
}
\examples{
\dontrun{
# Start a lesson (from blendtutor or any installed lesson package)
start_lesson("add_two_numbers")

# Start a lesson from a specific package
start_lesson("mypkg::my_lesson")

# Start a lesson from a local file (useful for development)
start_lesson("inst/lessons/my_lesson.yaml")

# Submit your code
submit_code("add_two <- function(x, y) { x + y }")
}
}
