Issue #99 — BYOK builder embedded API key
Build command:
  cargo run -p blendtutor-cli -- build --target webr --password testpass --embed-key fireworks:fw_test_embed_123 crates/core/tests/fixtures/r-course -o /tmp/bt-embed-probe
Build result: built 2 lesson(s) for webr into /tmp/bt-embed-probe (password-protected)

Server: python3 -m http.server 8899 --directory /tmp/bt-embed-probe
Server health: curl http://localhost:8899/index.html -> 200

Rodney session:
  uvx rodney start && uvx rodney open http://localhost:8899/index.html
  -> Chrome started; page title "blendtutor — password required"

Probe 1 — decrypt button exists:
  uvx rodney assert "document.getElementById('decrypt-button') !== null"
  -> pass

Probe 2 — enter password (Rodney has no `input` command; used `js` to set value):
  uvx rodney js "document.getElementById('decrypt-password').value = 'testpass'"
  -> testpass

Probe 3 — click decrypt:
  uvx rodney click "#decrypt-button"
  -> Clicked

Waited 5s for async decrypt + page render.

Probe 4 — lesson UI loaded (not still on decrypt shell):
  uvx rodney assert "document.querySelector('#lesson-select') !== null || document.querySelector('[data-action=submit]') !== null"
  -> pass

Probe 5 — key prompt NOT shown (embedded key should skip key-entry phase):
  uvx rodney assert "document.querySelector('[data-byok=key-prompt]') === null"
  -> pass

Probe 6 — embedded key stored in sessionStorage:
  uvx rodney assert "sessionStorage.getItem('fireworks_api_key') === 'fw_test_embed_123'"
  -> pass

Probe 7 — global __btEmbeddedKey cleared after applyEmbeddedKey:
  uvx rodney assert "window.__btEmbeddedKey === undefined"
  -> pass

Screenshots:
  docs/evidence/99/decrypt-shell-initial.png — initial password shell
  docs/evidence/99/embedded-key-decrypted.png — lesson UI after decrypt with embedded key

Verdict: PROBES_PASS
